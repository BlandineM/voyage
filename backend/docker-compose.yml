version: '3'
services:
  mysql:
    image: "mysql:5.7"
    container_name: voyage-db
    volumes:
      - ./db/data.sql:/docker-entrypoint-initdb.d/init.sql
    ports:
      - 3306:3306
    environment:
      MYSQL_ROOT_PASSWORD: ${PASSWORD}
volumes:
  db:
